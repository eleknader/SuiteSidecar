## LOCAL DEVELOPMENT ONLY
## Copy this file to connector-php/.env and fill values locally.
## Do NOT commit connector-php/.env.
## Runtime precedence: environment variables override values in config/profiles.php.

# Connector JWT secret (required for /auth/login and authenticated endpoints)
SUITESIDECAR_JWT_SECRET=replace-with-long-random-secret

# Optional JWT lifetime in seconds (default 28800 = 8h)
SUITESIDECAR_JWT_TTL_SECONDS=28800

# Optional session cleanup tuning
SUITESIDECAR_SESSION_TTL_SECONDS=28800
SUITESIDECAR_SESSION_GC_PROBABILITY_PERCENT=10
SUITESIDECAR_SESSION_GC_MAX_DELETES=50

# Profile secrets for profile id "example-dev" (normalized -> EXAMPLE_DEV)
SUITESIDECAR_EXAMPLE_DEV_CLIENT_ID=your-client-id
SUITESIDECAR_EXAMPLE_DEV_CLIENT_SECRET=your-client-secret

# Optional build metadata
# APP_GIT_SHA=
# APP_BUILD_TIME=
